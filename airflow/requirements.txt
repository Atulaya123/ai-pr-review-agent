# Airflow itself is installed separately, pinned against its own constraints
# file (see airflow/README.md) — these are just this DAG's own import chain
# (backend/core, backend/memory, backend/evaluation), same versions as
# backend/requirements.txt where they overlap.
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.6
openai>=1.54
anthropic>=0.39
google-genai>=1.0
